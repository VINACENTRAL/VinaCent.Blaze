class BlazeWebConstants {
    static PreviousAccount = 'PreviousAccount';
}

static class AvailableRegexs {
    static EmailChecker = /^[\w\-\.]+@([\w-]+\.)+[\w-]{2,4}$/gm;
    static EmailListSeprateByCommaChecker = /([\w\-\.]+@([\w-]+\.)+[\w-]{2,4}),? ?/gm
}